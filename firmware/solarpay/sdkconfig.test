# Overlay for automated end-to-end testing only. Use with:
#   idf.py -DSDKCONFIG_DEFAULTS="sdkconfig.defaults;sdkconfig.test" build
# Never merge these into sdkconfig.defaults -- see main/Kconfig.projbuild.
CONFIG_SOLARPAY_TEST_HARNESS=y
